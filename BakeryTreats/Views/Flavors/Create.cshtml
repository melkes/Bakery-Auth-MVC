@{
  Layout = "_Layout";
}

@model BakeryTreats.Models.Flavor

<h4>Add a new Flavor</h4>

<p>Go to @Html.ActionLink("this page", "Create", "Treats") to add a Treat.</p>

@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)
  
  @* @Html.LabelFor(model => model.Description)
  @Html.TextBoxFor(model => model.Description) *@

  <input type="submit" value="Add Flavors" class="btn btn-outline-primary" />
  <br/>
}
<p>@Html.ActionLink("Show all Flavors", "Index")</p>
